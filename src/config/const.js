import dotenv from "dotenv";
dotenv.config();

const {
  PORT,
  MONGO_URI,
  REDIS_HOST,
  REDIS_PORT,
  JWT_SECRET,
  JWT_EXPIRES_IN,
  AWS_SES_SMTP_HOST,
  AWS_SES_SMTP_USERNAME,
  AWS_SES_SMTP_PASSWORD,
  EMAIL_FROM,
  FRONTEND_APP_URL,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  FACEBOOK_APP_ID,
  FACEBOOK_APP_SECRET,
  ADMIN_EMAIL,
  BASE_URL,
  JWT_REFRESH_SECRET,
  NODE_ENV,
  PAYPAL_CLIENT_KEY,
  PAYPAL_SECRET_KEY,
  PAYPAL_MODE,
  SMTP_ENCRYPTION_KEY,
} = process.env;

export {
  PORT,
  MONGO_URI,
  REDIS_HOST,
  REDIS_PORT,
  JWT_SECRET,
  JWT_EXPIRES_IN,
  AWS_SES_SMTP_HOST,
  AWS_SES_SMTP_USERNAME,
  AWS_SES_SMTP_PASSWORD,
  EMAIL_FROM,
  FRONTEND_APP_URL,
GOOGLE_CLIENT_ID,
GOOGLE_CLIENT_SECRET,
FACEBOOK_APP_ID,
FACEBOOK_APP_SECRET,
ADMIN_EMAIL,
BASE_URL,
JWT_REFRESH_SECRET,
NODE_ENV,
  PAYPAL_CLIENT_KEY,
  PAYPAL_SECRET_KEY,
  PAYPAL_MODE,
  SMTP_ENCRYPTION_KEY,
};